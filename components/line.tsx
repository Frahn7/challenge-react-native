import { View } from "react-native";

export default function Line() {
  return (
    <View
      style={{
        borderBottomColor: "#ccc",
        borderBottomWidth: 1,
        width: "90%",
        alignSelf: "center",
        marginVertical: 10,
      }}
    />
  );
}
